version: '3.3'

services:

  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'author_matcher'
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - '3306:3306'
    expose:
      - '3306'


#  eresearch-author-matcher:
#    image: eresearch-author-matcher
#    build:
#      context: ./
#      dockerfile: Dockerfile
#    depends_on:
#      - mysql
#    ports:
#      - 8881:8881